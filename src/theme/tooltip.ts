// Tooltip Theme Configuration
// This file defines the styling for all tooltip components in the application.
// It provides a consistent look for informational tooltips.

// Base style configuration for tooltips
const baseStyle = {
  px: 3,                    // Horizontal padding
  py: 2,                    // Vertical padding
  bg: "gray.900",           // Dark background color
  borderRadius: "lg",       // Large border radius
};

// Export the complete tooltip theme configuration
const styles = {
  baseStyle,
};

export default styles;
